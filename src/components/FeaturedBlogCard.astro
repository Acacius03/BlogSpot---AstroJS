---
const { post } = Astro.props;
---

<article
	class='rounded-lg overflow-hidden bg-black border-[1px] border-gray-600'>
	<figure class='relative'>
		<img
			src={post.image}
			alt={post.title}
			class='w-full h-[300px] object-cover'
		/>
		<figcaption
			class='absolute p-4 inset-0 bg-gradient-to-b from-transparent to-black flex flex-col justify-end'>
			<h2 class='text-2xl font-bold'>{post.title}</h2>
			<small class='text-xs indent-0.5'>Posted on March 16, 2023</small>
			<p class='text-neutral-400 mt-2'>{post.description}</p>
		</figcaption>
	</figure>
	<section>
		<button class='bg-[#008080] font-bold py-2 text-lg w-full'>
			READ MORE
		</button>
	</section>
</article>
